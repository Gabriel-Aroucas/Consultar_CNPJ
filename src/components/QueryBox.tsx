import { styled } from "styled-components"

const QueryBox = () => {

    
    const Styled__queryBox = styled.a`
    color: #fff;
`

    return (
        <Styled__queryBox>
        </Styled__queryBox>
    )
}

export default QueryBox